replicaCount: 1

image:
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 4000  # Default for sockets

ingress:
  enabled: true
  className: nginx
  hosts: []  # Override in environment-specific values
  tls:
    - secretName: placeholder-tls-secret
      hosts: []

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  cpuUtilizationPercentage: 80
