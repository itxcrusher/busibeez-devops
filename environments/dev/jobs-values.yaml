replicaCount: 1

image:
  tag: dev

# Cron schedule for the job (adjust as needed)
schedule: "0 */6 * * *"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

configmap:
  ENVIRONMENT: dev
