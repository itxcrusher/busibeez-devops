replicaCount: 1

serviceName: support-socket

image:
  repository: registry.gitlab.com/astutexdevops/bb-latest/support-socket
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 4003

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: sandbox.busibeez.com
      paths:
        - path: "/support-socket(|$)(.*)"
          pathType: ImplementationSpecific
  tls:
    - secretName: dev-tls-secret
      hosts:
        - sandbox.busibeez.com

env:
  - name: SOCKET_PORT
    value: "4003"
  - name: PORT
    value: "4003"
  - name: NODE_ENV
    value: "development"

resources:
  requests:
    cpu: 25m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
